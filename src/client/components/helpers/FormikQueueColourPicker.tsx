import React from "react";
import { QueueTheme } from "../../generated/graphql";

type Props = {
    chosenTheme: QueueTheme;
    name: string;
};

export const FormikQueueColourPicker: React.FC<Props> = ({}) => {
    return <div></div>;
};
